services:
  # Backend Service
  - type: web
    name: vocab-smart-backend
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: PORT
        value: 8000
      - key: APP_NAME
        value: "Vocab Smart Backend"
      - key: APP_VERSION
        value: "1.0.0"
      - key: DEBUG
        value: false
      - key: ALLOWED_ORIGINS
        value: https://vocab-smart-frontend.onrender.com
      - key: API_PREFIX
        value: /api

  # Frontend Service
  - type: web
    name: vocab-smart-frontend
    env: static
    plan: free
    buildCommand: "npm install && npm run build"
    publishDir: dist
    envVars:
      - key: VITE_API_URL
        value: https://vocab-smart-backend.onrender.com
    routes:
      - type: rewrite
        source: /api/*
        destination: https://vocab-smart-backend.onrender.com/api/*
